import { ProductCreate } from '@/modules/Admin/Products';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Product create',
  description: 'This is product create page',
};

export default ProductCreate;
